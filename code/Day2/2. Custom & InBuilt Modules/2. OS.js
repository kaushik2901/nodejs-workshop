// const os = require('os');

// // os.EOL // gives the line delimiter sequence. It's \n on Linux and macOS, and \r\n on Windows.

// console.log(os.arch()) // Return the string that identifies the underlying architecture, like arm, x64, arm64.

// console.log(os.cpus()) // Return information on the CPUs available on your system.

// console.log(os.freemem()) // Return the number of bytes that represent the free memory in the system.

// console.log(os.totalmem()) //Returns the number of bytes that represent the total memory available in the system.

// console.log(os.homedir()) // Return the path to the home directory of the current user.

// console.log(os.hostname()) //Return the host name.

// console.log(os.networkInterfaces()) // Returns the details of the network interfaces available on your system.

// console.log(os.platform()) // Return the platform that Node.js was compiled for:

// console.log(os.release()) // Returns a string that identifies the operating system release number

// console.log(os.tmpdir()) // Returns the path to the assigned temp folder.

// console.log(os.type()) // Identifies the operating system:

// console.log(os.uptime()) // Returns the number of seconds the computer has been running since it was last rebooted.

// console.log(os.userInfo()) // Returns an object that contains the current username, uid, gid, shell, and homedirdata